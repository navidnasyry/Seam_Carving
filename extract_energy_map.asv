function total_energy_map = extract_energy_map(input_image)
    
    height = size(I_input, 1);
    width = size(I_input, 2);
    total_energy_map = zeros(height, width);

    total_energy_map = energy_gradient(total_energy_map, input_image);
    
    


end

